export interface TransactionsCardType {
  addressTo: string;
  addressFrom: string;
  timestamp: string;
  message: string;
  keyword: string;
  amount: string;
}

export interface ServiceCardType {
  color: string;
  title: string;
  icon: string;
  subtitle: string;
}